const axios=require("axios");
const has=(text,words)=>words.some(w=>text.includes(w));
const fallbackAnalyze=({title="",description=""})=>{const text=`${title} ${description}`.toLowerCase();let priority="low";if(has(text,["account hacked","security issue","data loss","production outage","system down","cannot access account","payment failed repeatedly"]))priority="critical";else if(has(text,["billing error","login problem","broken feature","major bug","blocked","urgent"]))priority="high";else if(has(text,["slow","configuration","invoice","product issue"]))priority="medium";let category="General Inquiry";if(has(text,["payment","billing","invoice","refund"]))category="Billing";else if(has(text,["login","password","account","access","hacked"]))category="Account Access";else if(has(text,["bug","error","crash","broken"]))category="Bug Report";else if(has(text,["feature","request","enhancement"]))category="Feature Request";else if(has(text,["technical","api","server","integration"]))category="Technical Issue";let sentiment="Neutral";if(has(text,["angry","furious","terrible","worst"]))sentiment="Angry";else if(has(text,["urgent","immediately","asap","critical"]))sentiment="Urgent";else if(has(text,["bad","failed","problem","issue","broken"]))sentiment="Negative";else if(has(text,["thanks","great","good","helpful"]))sentiment="Positive";const aiTags=[...new Set(["login","billing","bug","refund","account","urgent","api","feature"].filter(tag=>text.includes(tag)))];const aiSummary=description.split(/[.!?]/).filter(Boolean).slice(0,2).join(". ").trim()||title;return{aiSuggestedCategory:category,aiSuggestedPriority:priority,sentiment,aiSummary,aiTags,recommendedTone:priority==="critical"||sentiment==="Angry"?"Empathetic, urgent, and reassuring":"Professional and helpful"}};
const analyzeTicket=async(payload)=>{if(process.env.ENABLE_AI_SERVICE==="true"){try{const{data}=await axios.post(`${process.env.AI_SERVICE_URL}/analyze-ticket`,payload,{timeout:4000});return data}catch(e){console.warn("AI service unavailable, fallback used")}}return fallbackAnalyze(payload)};
module.exports={analyzeTicket,fallbackAnalyze};
