import{useState}from"react";import{toast}from"react-toastify";import api from"../../services/api";const CreateTicket=()=>{const[form,setForm]=useState({title:"",description:""});const[loading,setLoading]=useState(false);const submit=async e=>{e.preventDefault();if(form.title.length<5)return toast.error("Title must be at least 5 characters");if(form.description.length<20)return toast.error("Description must be at least 20 characters");try{setLoading(true);await api.post("/tickets",form);toast.success("Ticket created and AI triaged");setForm({title:"",description:""})}catch(err){toast.error(err.response?.data?.message||"Ticket creation failed")}finally{setLoading(false)}};return <div><h1 className="text-3xl font-black">Create Ticket</h1><form onSubmit={submit} className="card mt-6 grid gap-4 p-6"><input className="input" placeholder="Ticket title" value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/><textarea className="input min-h-44" placeholder="Describe your issue..." value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/><button disabled={loading} className="btn-primary">{loading?"Analyzing...":"Submit Ticket"}</button></form></div>};export default CreateTicket;
