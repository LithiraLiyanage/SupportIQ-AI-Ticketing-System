import{useState}from"react";import{useNavigate,Link}from"react-router-dom";import{toast}from"react-toastify";import{useAuth}from"../context/AuthContext";const Register=()=>{const{register}=useAuth();const navigate=useNavigate();const[form,setForm]=useState({name:"",email:"",password:"",confirm:""});const submit=async e=>{e.preventDefault();if(form.password!==form.confirm)return toast.error("Passwords do not match");try{const path=await register({name:form.name,email:form.email,password:form.password});toast.success("Registered");navigate(path)}catch(err){toast.error(err.response?.data?.message||"Registration failed")}};return <main className="flex min-h-[80vh] items-center justify-center px-4 py-12"><form onSubmit={submit} className="card w-full max-w-lg p-8"><h1 className="text-3xl font-black">Create account</h1><input className="input mt-6" placeholder="Full name" onChange={e=>setForm({...form,name:e.target.value})}/><input className="input mt-4" placeholder="Email" type="email" onChange={e=>setForm({...form,email:e.target.value})}/><input className="input mt-4" placeholder="Password" type="password" onChange={e=>setForm({...form,password:e.target.value})}/><input className="input mt-4" placeholder="Confirm password" type="password" onChange={e=>setForm({...form,confirm:e.target.value})}/><button className="btn-primary mt-6 w-full">Register</button><p className="mt-5 text-center text-sm text-slate-500">Already have account? <Link className="font-bold text-primary" to="/login">Login</Link></p></form></main>};export default Register;
